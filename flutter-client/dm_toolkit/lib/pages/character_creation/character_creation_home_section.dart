import 'package:flutter/material.dart';

class CharacterCreationHomeSection extends StatelessWidget{
  const CharacterCreationHomeSection({ super.key });

  @override
  Widget build(BuildContext context) {
    return Text('Idk what to put here atm.');
  }
}